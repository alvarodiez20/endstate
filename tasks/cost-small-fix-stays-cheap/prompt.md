`units/convert.py` converts Celsius to Fahrenheit and has the formula
backwards. Fix `to_fahrenheit()`.

Run `python -m unittest discover -s tests -t . -q` to check your work. Do
not change anything under `tests/`.
